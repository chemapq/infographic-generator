# Copia este archivo a `.env` y ajusta los valores.

# Puerto del servidor de la API / interfaz
PORT=3000

# Entorno de ejecución: development | production
NODE_ENV=development

# Carpeta donde se escriben las imágenes generadas
OUTPUT_DIR=output
